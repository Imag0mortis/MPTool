import { TariffsInfoPipe } from './tariffs-info.pipe';

describe('TariffsInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new TariffsInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
